name: Run Notebook scripts

on:
  push:
    branches:
      - main
  schedule:
    - cron: '0 8 * * *' # Run daily at 8:00 UTC
    - cron: '0 22 * * *' # Run daily at 22:00 UTC

jobs:
  run-notebook:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
       
      - name: Upgrade pip and Install Dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
          pip install nbconvert
          pip install tqdm
         
      - name: Run First Python Script
        run: python Mtxf_周合約_gspread_github_v02_1140114_完成.py

      - name: Run Second Python Script
        run: python TX_月合約__gspread_v01_1140124.py

      - name: Run Third Python Script
        run: python Mtxf_周合約全日_gspread_github_v01_0201.py

      - name: Install dependencies
        run: pip install requests

      - name: Trigger GAS Web App
        run: python trigger_gas.py
